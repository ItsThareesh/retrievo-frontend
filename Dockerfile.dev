FROM node:20-alpine

WORKDIR /app

# Install dependencies in separate layer for caching
# This layer only rebuilds when package files change
COPY package.json package-lock.json ./
RUN npm ci

# In dev, this will be overridden by volume mount
# So COPY . . isn't needed here

EXPOSE 3000

CMD ["npm", "run", "dev"]
